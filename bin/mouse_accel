#!/bin/bash

die () { echo >&2 $@; exit 1; }

# xinput set-prop 'Logitech USB Gaming Mouse' 'Device Accel Profile' -1 || die "Mouse is not plugged in"
xinput set-prop 'Logitech M705' 'Device Accel Profile' -1 || die "Mouse is not plugged in"
